"""Client layer for MeetingScribe v2 (skeleton).

This package contains the CLI client that can talk to the daemon when
available and gracefully falls back to direct execution (v1 behavior).

Phase 1 scope: minimal status command with fallback executor.
"""

