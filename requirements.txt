# MeetingScribe - High-Quality Audio Recording
# Core dependencies for Teams meeting recording

# Configuration and Logging
loguru==0.7.3
python-dotenv==1.1.1
pydantic==2.11.7
pydantic-settings==2.10.1

# System Monitoring
psutil==6.1.1

# Audio Recording (WASAPI)
pyaudiowpatch==0.2.12.7

# Windows Integration (Teams detection)
wmi==1.5.1
pywin32>=307

# Audio Processing and Transcription
google-generativeai==0.8.5  # Google Gemini API for transcription
soundfile==0.13.1  # Audio file I/O
scipy==1.15.3  # Signal processing for audio optimization
pydub==0.25.1  # Audio format conversion (requires ffmpeg)
numpy>=1.23.5,<2.5  # Array operations for audio processing

# Development and Testing
pytest==8.4.0

# Notes:
# - pyaudiowpatch provides WASAPI loopback support (Windows)
# - google-generativeai enables cloud transcription via Gemini API
# - soundfile/scipy for audio optimization before upload
# - pydub requires ffmpeg for non-WAV formats (M4A, MP3, etc.)
# - Raycast extension uses Node.js (see raycast-extension/package.json)
