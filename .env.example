# MeetingScribe - Configuração de Exemplo
# Copie este arquivo para .env e ajuste as configurações conforme necessário

# =============================================================================
# CONFIGURAÇÕES DA APLICAÇÃO
# =============================================================================

# Nome e versão da aplicação
APP_NAME=MeetingScribe
APP_VERSION=1.0.0

# Modo de desenvolvimento (true/false)
DEBUG=false

# Nível de log (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# =============================================================================
# CAMINHOS E DIRETÓRIOS
# =============================================================================

# Diretório base do projeto (normalmente "./")
BASE_DIR=./

# Diretório para armazenar gravações, transcrições e exports
STORAGE_DIR=./storage

# Diretório para cache de modelos Whisper
MODELS_DIR=./models

# Diretório para arquivos de log
LOGS_DIR=./logs

# =============================================================================
# CONFIGURAÇÕES DE ÁUDIO
# =============================================================================

# Taxa de amostragem para gravação (Hz)
# Valores comuns: 16000 (padrão Whisper), 44100 (CD quality), 48000 (professional)
AUDIO_SAMPLE_RATE=44100

# Número de canais de áudio
# 1 = mono, 2 = stereo
AUDIO_CHANNELS=2

# Tamanho do chunk para processamento (samples)
CHUNK_SIZE=1024

# Duração do buffer em segundos
BUFFER_DURATION=30

# =============================================================================
# CONFIGURAÇÕES DO WHISPER (IA DE TRANSCRIÇÃO)
# =============================================================================

# Modelo Whisper a usar por padrão
# Opções: tiny, base, small, medium, large-v3
# tiny    = ~39MB,  mais rápido, menor precisão
# base    = ~74MB,  equilibrio ideal (recomendado)
# small   = ~244MB, boa precisão, velocidade moderada  
# medium  = ~769MB, alta precisão, mais lento
# large-v3= ~1.5GB, máxima precisão, muito lento
WHISPER_MODEL=base

# Idioma padrão para transcrição
# auto = detecção automática
# pt = português, en = inglês, es = espanhol, etc.
WHISPER_LANGUAGE=auto

# Dispositivo para processamento
# auto = detecção automática (GPU se disponível, senão CPU)
# cpu = forçar uso de CPU
# cuda = forçar uso de GPU (requer CUDA)
WHISPER_DEVICE=auto

# Tipo de computação para modelos
# float16 = mais rápido, menos memória (recomendado para GPU)
# float32 = mais preciso, mais memória (recomendado para CPU)
# int8 = muito rápido, menor qualidade
WHISPER_COMPUTE_TYPE=float16

# Beam size para decodificação (1-10)
# Valores maiores = melhor qualidade, mais lento
WHISPER_BEAM_SIZE=5

# Temperatura para sampling (0.0-1.0)
# 0.0 = determinístico, 1.0 = mais variação
WHISPER_TEMPERATURE=0.0

# =============================================================================
# CONFIGURAÇÕES AVANÇADAS DE ÁUDIO
# =============================================================================

# Threshold para compressão de áudio
COMPRESSION_RATIO_THRESHOLD=2.4

# Threshold para probabilidade de log
LOG_PROB_THRESHOLD=-1.0

# Threshold para detecção de silêncio
NO_SPEECH_THRESHOLD=0.6

# Condicionar em texto anterior (true/false)
CONDITION_ON_PREVIOUS_TEXT=true

# Gerar timestamps de palavras (true/false)
WORD_TIMESTAMPS=true

# Usar filtro VAD (Voice Activity Detection) (true/false)
VAD_FILTER=true

# =============================================================================
# CONFIGURAÇÕES DE EXPORTAÇÃO
# =============================================================================

# Incluir metadados por padrão (true/false)
EXPORT_INCLUDE_METADATA=true

# Incluir timestamps por padrão (true/false)
EXPORT_INCLUDE_TIMESTAMPS=true

# Formato de exportação padrão (txt, json, srt, vtt, xml, csv)
EXPORT_DEFAULT_FORMAT=txt

# =============================================================================
# CONFIGURAÇÕES DE PERFORMANCE
# =============================================================================

# Máximo de threads para processamento paralelo
MAX_WORKERS=4

# Limite de memória em MB para cache de modelos
MODEL_CACHE_LIMIT=2048

# =============================================================================
# CONFIGURAÇÕES DE DESENVOLVIMENTO (apenas para DEBUG=true)
# =============================================================================

# Mostrar informações detalhadas de debug
VERBOSE_DEBUG=false

# Salvar arquivos temporários para debug
SAVE_DEBUG_FILES=false

# Profiling de performance
ENABLE_PROFILING=false

# =============================================================================
# NOTAS IMPORTANTES
# =============================================================================

# 1. Para melhor performance em computadores lentos:
#    WHISPER_MODEL=tiny
#    WHISPER_COMPUTE_TYPE=float32
#    WHISPER_DEVICE=cpu

# 2. Para máxima qualidade em computadores potentes:
#    WHISPER_MODEL=large-v3
#    WHISPER_COMPUTE_TYPE=float16
#    WHISPER_DEVICE=cuda

# 3. Para gravações de reuniões longas:
#    BUFFER_DURATION=60
#    CHUNK_SIZE=2048

# 4. Para processamento batch:
#    MAX_WORKERS=8
#    MODEL_CACHE_LIMIT=4096